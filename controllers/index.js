const todoController = require('./todos');
const todoItemController = require('./todoItems');
const userController = require('./users');

module.exports = {
  todoController,
  todoItemController,
  userController,
};
